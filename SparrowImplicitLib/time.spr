module time

[rtct]
    [native("clock")] fun clock: Int

    class Timer
        var _startTime: Int

        fun ctor { _startTime = clock }
        fun ctor(other: Timer) { _startTime = other._startTime }

        fun elapsed = Double(clock - _startTime) / 1000.0
        fun restart { _startTime = clock }
