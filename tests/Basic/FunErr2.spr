//!! -t "SparrowImplicitLib.spr"

/*
a
b
c
d
*/
injectBackendCodeRt("
1
2
3
4
");

class Test
{
    var x: Int;
    var y: Int;
    
    fun[static] f()
    {
        writeLnInt(1);
    }
    fun[static] g(n: Int)
    {
        writeLnInt(n);
    }
    
    fun m1()
    {
        writeLnInt(2);
    }
    
    fun m2(n: Int)
    {
        writeLnInt(n);
    }

    
    fun[static] staticThisTest()
    {
        x;          // ERROR
        y;          // ERROR
        this.x;     // ERROR
        this.y;     // ERROR
    }
}

fun f();
//fun[static] g();    // E RROR

var a: Test;

fun h();
fun h(a: Int);
fun h(a: Int@);
fun h(a: (Int@)@);
fun h(a: Double@);
fun h(a: Double);

fun[native("test")] test(n: Int)
{
    assignInt(a.x, 10);
    assignInt(a.y, 11);

    Test.f();
    Test.g(5);
    a.f();          // ERROR
    a.g(5);         // ERROR
    
    Test.f(10);     // ERROR
    Test.g(10,10);  // ERROR
    
    Test.m1();      // ERROR
    Test.m2(10);    // ERROR
    a.m1();
    a.m2(10);

    h();
    h(1);           // ok
    h(1.0);         // ok
    h('a');         // ERROR
}
