//!! -t "SparrowImplicitLib.spr" -fno-main

class Test
{
    var x: Int;
    var y: Int;

    [static, rt] fun f()
    {
        writeLnInt(1);
    }
    [static, rt] fun g(n: Int)
    {
        writeLnInt(n);
    }
}

var a: Test;

[native("test")] fun test(n: Int)
{
    assignInt(a.x, 10);
    assignInt(a.y, 11);

    Test.f();
    Test.g(5);
}

/*<<<Running()
1
5
>>>*/